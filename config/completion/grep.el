;;; grep.el --- Consult configuration -*- no-byte-compile: t; lexical-binding: t; -*-

;; Writable Grep:
;; wgrep: Allows editing of grep buffers, enabling changes to be applied directly to files.

(use-package wgrep)
